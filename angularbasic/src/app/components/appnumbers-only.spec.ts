import { AppnumbersOnly } from './appnumbers-only';

describe('AppnumbersOnly', () => {
  it('should create an instance', () => {
    const directive = new AppnumbersOnly();
    expect(directive).toBeTruthy();
  });
});
